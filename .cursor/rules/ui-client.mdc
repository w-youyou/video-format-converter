---
description: PySide6 桌面客户端界面与线程规则
globs: app/ui/**/*.py
alwaysApply: false
---

# 界面

四区布局：文件/拖放、缩略图、信息、底部操作（预设/路径/进度/主按钮）。

- 主按钮同时只表达一个动作：开始 / 进行中 / 打开文件夹
- 缩略图保持比例；长路径省略 + tooltip；1280×720 下控件不可被裁切
- 禁止把 ffmpeg 命令当作主操作；命令只进收起的日志
- UI 禁止拼接 ffmpeg 参数，只调 `core` / worker
- 进行中禁用「开始」，允许取消；退出进行中任务要确认
- **closeEvent 等所有 worker 收尾**（不只是转换中）
- 完成后展示体积变化（S7）；切预设只换输出后缀；输出不得等于源文件
- 无音轨选提取 MP3 时立刻提示并禁用开始
- 错误用中文说明下一步，禁止只 print
